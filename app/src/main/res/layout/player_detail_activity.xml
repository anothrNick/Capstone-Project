<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent" android:background="#EFEFEF"
    tools:context="com.dev.nick.scorch.players.PlayerDetailActivity">

    <!-- Tabs and view pager for different info -->
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/player_info"
        android:orientation="vertical"
        android:background="#FFF">

        <android.support.design.widget.TabLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/tabanim_tabs"
            app:tabIndicatorColor="@color/primary"/>

        <android.support.v4.view.ViewPager
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:id="@+id/tabanim_viewpager" />

    </LinearLayout>

    <!-- header section with player position -->
    <FrameLayout
        android:layout_width="fill_parent"
        android:background="#EFEFEF"
        android:layout_height="180dp"
        android:id="@+id/player_info">

        <FrameLayout
            android:layout_width="fill_parent"
            android:layout_height="140dp"
            android:background="@drawable/beam_center"
            android:id="@+id/colorHeader" />
<!--
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="160dp"
            android:layout_marginTop="-15dp"
            android:layout_marginRight="-20dp"
            android:id="@+id/playerPositionWrap"
            android:background="@drawable/player_position"
            android:layout_gravity="right">

            <TextView android:text="1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/player_position"
                android:textSize="112sp"
                android:paddingTop="5dp"
                android:paddingLeft="80dp"
                android:paddingRight="80dp"
                android:textColor="#66ECECEC"/>
        </FrameLayout>-->
        <!-- player image -->
        <RelativeLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent">

            <ImageView
                android:id="@+id/player_icon"
                android:src="@drawable/ic_player"
                android:layout_marginTop="60dp"
                android:layout_marginLeft="16dp"
                android:background="#FFF"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:elevation="2dp"
                android:padding="3dp"
                android:translationZ="4dp"/>

        <!-- player name -->


            <TextView android:text="no player"
                android:layout_toRightOf="@+id/player_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="60dp"
                android:layout_marginLeft="10dp"
                android:id="@+id/player_name"
                android:textColor="@color/white"
                android:textSize="24sp"/>

        </RelativeLayout>

    </FrameLayout>


</RelativeLayout>
